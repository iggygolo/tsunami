# Tsunami Multi-Artist Platform Configuration
# This file shows the minimal configuration needed for Tsunami
# All artist-specific settings are now managed through the Artist Settings UI

# =============================================================================
# MINIMAL CONFIGURATION
# =============================================================================
# No environment variables are required for basic functionality
# All configuration is now handled per-artist through the studio interface

# Optional: You can still set these if you want to override defaults
# (But they are not required - the system will work without any environment variables)

# Example of completely empty configuration - this works fine:
# (No variables needed)